# AI 学习日志

我的三个月 AI 学习计划：
- 第 1 周：环境搭建 + 第一个 AI 对话应用（进行中）
- 目标：用 AI 解决问题，不做算法工程师

第 1 天：装好了 Python / Git / VS Code，成功 clone 了这个仓库！
-以上是第一天的记录，明天开始亲手学git

第2天：学习了git五条命令
git clone ：克隆仓库到本地
git status：查看仓库的状态
git add：把改动登记
git commit：存档
git push：把改动上传云端
git pull：把云端的内容下拉
2026年8/18：我在网页上写下这行字，用来练习 git pull。

第3天：学习了DEEPSEEK的API调用

接口地址
身份认证
请求参数
发送请求
接收响应
处理结果

第四天：给程序添加了查询对话轮数的功能

用 elif 加新分支、len() 数列表、f-string 填值、continue 跳回循环
// 整除、(总数-1)//2 计算轮数、注释解释代码
IndentationError（缩进错误）：Python 用缩进划分代码块，缩进不齐就报错

if __name__ == "__main__": = "只有被直接运行时才开跑，被借用时别乱动"
为什么需要它：防止 import 时误触发 main()

第5天：学习了Prompt四大基本功 + JSON

实验1 角色设定：AI的回答风格随人设改变
  /system 你是一位20年经验的A股交易员 → 实战大白话
  /system 你是一位零基础讲师 → 简单打比方
  结论：想要什么风格，就给它什么人设

实验2 Few-shot（给例子）：可以限定AI的输出格式
  例：句子 → 正面/负面，AI就严格按例子输出
  结论：给AI看样板，它就会照着样板输出

实验3 思维链CoT：复杂问题让AI一步一步想
  结论：简单问题不用，计算/逻辑/多步骤问题要用

实验4 JSON输出：结构化数据，程序可以直接解析
  JSON是什么：人和程序都能读的数据格式
  两种骨架：{}对象（按名字查）[]数组（按顺序查）
  可以嵌套：对象套数组，数组套对象

JSON练习：写了自己的模拟持仓（3只股票）
  逗号规则：键值对之间用逗号，最后一个不加
  踩坑：全角逗号，会被程序报错！写代码要切英文输入法
  数字不加引号才是数字，加引号是文字

明天：周六实战——让AI把长文总结成JSON要点


第六天：

学习了python语法

第6天（第2周周二）：列表/字典 + 词频统计

复习：
- 列表 []：按位置取 [0]、[1]
- 字典 {}：按名字取 ["名称"]
- 一句话：列表是一排，字典是带名字的表

新知识：
- 往字典加值：统计["茅台"] = 1（没有就新增）
- 更新值：统计["茅台"] = 统计["茅台"] + 1（已有就覆盖）
- if 词 in 字典：判断键是否存在
- split()：按空格把文字拆成列表

词频统计程序（word_count.py）：统计一段话每个词出现几次
输出：茅台 3 次 / 五粮液 2 次 / 宁德 1 次 ✅

重要发现：中文没有空格，split() 直接失效！
- 中文分词需要专门工具（如 jieba），这是第10周的伏笔
- 认知：中文处理 ≠ 英文处理，分词是专门领域

第7天（第2周周三）：函数 + 文件读写

函数：
- def 名字(参数): 定义函数
- return 值：把结果交出去
- 调用：名字(实参)
- 核心思想：写一次用多次，改一处全生效
- 练习：加税函数 100*1.13 = 113 ✅

文件读写：
- 读：open("文件","r") → read()/readlines() → close()
- 写：open("文件","w") → write() → close()（w会覆盖！）
- encoding="utf-8" 必须写，否则中文乱码
- with open() as 文件：用完自动关，推荐

成绩平均分程序（score_avg.py）：
- readlines() 按行读
- [1:] 跳过表头
- split(",") 拆CSV
- int() 字符串转数字
- 计算每个学生平均分，写入结果.txt
- 我改进了：加显示总分（253/275/241）

里程碑：第一次实现"读文件→处理→写文件"完整闭环

第8天（第2周周四）：异常处理——读懂报错

Traceback（事故报告）三要素：
- File "xx.py", line N → 哪个文件第几行
- 出错的那行代码
- 错误类型 + 说明（最后一行最重要！）

6种常见错误：
- TypeError 类型错误（字符串+数字）
- NameError 名字未定义（拼错变量名）
- IndexError 下标越界（列表[5]但只有3个）
- KeyError 键不存在（字典["没有的键"]）
- FileNotFoundError 文件找不到
- IndentationError 缩进错误（之前踩过）

try/except 让程序出错也不崩：
- try: 尝试做
- except 某类型: 接住这种错误
- except Exception as 错误: 兜底接住所有

技巧：Ctrl+/ 快速注释/取消注释（调试排查超好用）
心法：报错=Python在告诉我哪错了，读懂了就是免费老师

第9天（第2周周五）：akshare下载A股数据 ★量化数据水管接通★

安装：pip install akshare（免费开源A股数据源）

核心函数：
- ak.stock_zh_a_hist(symbol="600519", period="daily", start_date, end_date)
- 返回 DataFrame（Pandas表格）→ 明天第3周正式学

下载器（download_stocks.py）：
- 元组 ("名称", "代码") 存一对数据
- for 名称, 代码 in 列表：自动拆包
- df.to_csv("文件.csv", index=False, encoding="utf-8-sig")
- utf-8-sig：Excel打开中文不乱码（写CSV标准姿势）

成果：下载5只股票真实日线，茅台1213行（2020至今）
- 每行是真实行情：日期/开盘/收盘/最高/最低/成交量

认知：
- 调akshare API = 调DeepSeek API，同一套"调API拿数据"模式
- 行业习惯：代码入库，数据不入库（随时可重新下载）

明天：Pandas处理真实茅台数据（均线/筛选/分组/画图）！

第10天（第2周周六）：数据整理脚本——给股票CSV加涨跌幅列

知识点：
- import csv：csv.reader 读成"行的列表"，csv.writer 写回
- 涨跌幅 = (今收-昨收)/昨收×100
- 取"上一行"：行列表[i-1]（涨跌幅核心逻辑）
- 第一条数据没有昨天，涨跌幅留空
- 列索引：0日期 1代码 2开盘 3收盘 4最高 5最低 6成交量 9涨跌幅
- float() 字符串转小数（CSV读出来全是字符串）

踩坑记录：
- 收盘价在索引3不是2！取错列报错 '开盘'
- 排查法：打印行列表[0]/[1] 看数据结构 → 再修代码
- 黄金法则：先看数据，再写代码

验证：自己算的涨跌幅 vs akshare自带涨跌幅 → 一致 ✅
- 习惯：不算完就信，等于没算

第11天（第3周周一）：Pandas入门——处理茅台真实数据

安装：pip install pandas matplotlib

核心概念：DataFrame = 带名字的二维表
- 每行 = 一天行情（有行号）
- 每列 = 一个字段（有列名，按名字取！）
- 我上周踩的"收盘在索引3"的坑，Pandas用 df["收盘"] 直接解决

三个核心操作：
- pd.read_csv("文件")：读CSV变DataFrame（一行顶上周10行代码）
- df.head()：看前5行；df.info()：数据质量检查（每列类型/非空数量）
- df.describe()：统计摘要（均值/最大/最小/分位数）
- df["收盘"]：按列名取一列（Series）

真实数据观察（茅台2020至今，1213行）：
- 1213 non-null 全部无缺失 ✅ 数据健康
- 收盘均值 1735.71，最高 2627.88（2021年巅峰）
- 2020-01-03 大跌 -4.55%（真实历史，疫情影响）

验证：涨跌幅公式在真实数据上核对 ✅

认知：Pandas 3行 = 上周csv模块20行 → 站在巨人肩膀上

第12天（第3周周二）：Pandas筛选/排序/分组

筛选：
- df[df["涨跌幅"]>5]：条件筛行（True/False名单对齐每行）
- & 表示"且"（不是and）
- 放量(>200亿)且上涨(>2%)：茅台只有2天
- 记忆：方括号里单个名字=单列(Series)，名字列表=多列(DataFrame)

排序：
- df.sort_values("列")：从小到大；ascending=False：从大到小
- 茅台跌幅最大：2020-07-16 -7.90%
- 2024-10-09 同时出现在跌幅最大和成交额最大（巨量暴跌）

groupby分组：
- df["年份"]=df["日期"].str[:4]：新增列（截取前4位）
- df.groupby("年份")["涨跌幅"].mean()：每年均值
- 广播：df["列"]=单个值 → 每行都是它
- 发现：茅台2020涨2022跌，价格2021见顶1970，每年约243个交易日

数据故事：日均涨跌幅≈0 → 印证"预测每日涨跌≈抛硬币"
